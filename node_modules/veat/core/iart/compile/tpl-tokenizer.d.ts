/// <reference types="veat/veat" />
/**
 * 将模板转换为 Tokens
 * @param       source
 * @param    rules     @see defaults.rules
 * @param      context
 * @return
 */
declare const tplTokenizer: {
    (source: string, rules: any, context?: {}): any[];
    TYPE_STRING: string;
    TYPE_EXPRESSION: string;
    TYPE_RAW: string;
    TYPE_ESCAPE: string;
};
export default tplTokenizer;
