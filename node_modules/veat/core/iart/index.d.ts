/// <reference types="veat/veat" />
declare class Art {
    defopt: ArtDefaults;
    constructor(settings: ArtDefaults);
    /**
     * 模板引擎
     *
     * 当传入的是 DOM ID 时, 则使用 document.getElementById('domid') 来获取模板内容
     *
     * 当传入的是可访问的 url 时, 则使用 fetch api 进行模板内容获取
     *
     * 当传入本地字符串模板时, 需要将本地模板字符串转换成 DataURL, 在进行传入, 底层使用的还是 fetch api
     *
     * @param id_or_url 存放模板内容的路径 url 或 其容器的 DOM id 或 转化成 DataURL 的本地模板字符串
     * @param data 需填充的数据
     * @param options 选项
     * @return 模板渲染后的内容
     */
    render(id_or_url: string, data: OType<any>, options?: ArtDefaults): Promise<string>;
}
declare const _default: Art;
export default _default;
