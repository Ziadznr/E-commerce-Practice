/// <reference types="veat/veat" />
/**
 * 16 进制字符串与普通字符串之间的简单转换
 */
declare class Hexs {
    private textEncoder;
    private textDecoder;
    constructor();
    /**
     * 将目标 16 进制的字符串, 转换成普通字符串
     * @param str 目标 16 进制字符串
     * @returns 转换后的普通字符串
     */
    hexToStr(str: string): string;
    /**
     * 将目标普通的字符串转换成 16 进制字符串
     * @param str 目标普通的字符串
     * @returns 转换后的16进制字符串
     */
    strToHex(str: string): string;
}
declare const _default: Hexs;
export default _default;
