/// <reference types="veat/veat" />
declare class Compressors {
    private type;
    constructor();
    toDataURL(file: File, quality?: number): Promise<string>;
    toBlob(file: File, quality?: number): Promise<Blob>;
    /**
     * 压缩图片
     * @param file 文件 或 是base64格式的url
     * @returns 压缩后的图片
     */
    private exc;
    private draw;
}
declare const _default: Compressors;
export default _default;
