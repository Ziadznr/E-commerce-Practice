/// <reference types="veat/veat" />
declare class Strings {
    constructor();
    /**
     * 字符串格式化(用双大括号作为标识)
     * @param tpl 模板
     * @param data 数据
     * @returns 绑定数据后的字符串
     */
    format(tpl: string, data: OType<any>): string;
    /**
     * 生成uuid
     * @returns uuid;
     */
    uuid(): string;
    /**
     * 驼峰格式
     * @param input home-handler
     * @returns homeHandler
     */
    camelCase(input: string): string;
}
declare const _default: Strings;
export default _default;
