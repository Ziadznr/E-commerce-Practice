/// <reference types="veat/veat" />
/**
 * 简单校验车牌号
 * @param lpnNo 车牌号
 * @returns 是否合法
 */
declare function checkLPN(lpnNo: string): boolean;
/**
 * 简单校验手机号码
 * @param telNo 手机号码
 * @returns 是否合法
 */
declare function checkTelNo(telNo: string): boolean;
/**
 * 校验身份证号码是否合法
 * @param cardid 身份证号码
 * @returns 是否合法
 */
declare function checkCardId(cardid: string): boolean;
/**
 * 通过身份证号码获取生日信息
 * @param cardid 身份证号码
 * @param separator 分隔符(默认'-')
 * @returns 格式化的生日信息
 */
declare function getBirthday(cardid: string, separator?: string): string;
export { checkCardId, getBirthday, checkLPN, checkTelNo };
