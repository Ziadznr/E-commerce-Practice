/**
 * 载入子模板
 */
const include = async (key, data, options) => {
    const module = await import('../index'), compile = module.default, sublist = options.sublist || {}, id_or_url = sublist[key];
    return await compile(id_or_url, data, options);
};
export default include;
